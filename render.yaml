services:
  - type: web
    name: ocr-api
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 10000"
    disk:
      name: ocr-swap
      mountPath: /swap
      sizeGB: 1
      readOnly: false
